# Local credentials / environment configuration
config.env
.config.env
.env

# OS files
.DS_Store
Thumbs.db

# Node modules / dependencies (if any are added later)
node_modules/
dist/
.env.local
.env.*.local

# SQL Setup
supabase_setup.sql
add_quality_metrics.sql
add_reimbursements_table.sql